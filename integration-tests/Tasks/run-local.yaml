apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: run-local
spec:
  steps:
    - name: run-podman
      image: quay.io/konflux_ui_qe/konflux-ui-tests:latest
      onError: continue
      workingDir: /var/workdir
      script: |
        #!/bin/sh
        ls
        exit 0